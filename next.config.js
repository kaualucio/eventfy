/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'github.com',
      'lh3.googleusercontent.com',
      'files.edgestore.dev',
    ]
    }
}

module.exports = nextConfig
